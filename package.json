{
  "name": "100mscript",
  "private": true,
  "version": "0.2.0",
  "description": "100mscript desktop app (Electron frontend + Zig backend)",
  "scripts": {
    "dev": "npm --prefix electron_app run dev",
    "check": "npm --prefix electron_app run check && (cd zig_backend && zig build test)",
    "build:zig": "cd zig_backend && zig build",
    "dist:win": "npm --prefix electron_app run dist:win"
  }
}
